# Local assistant config - not part of the project
.claude/
.claudeignore
CLAUDE.md
CLAUDE.local.md
graphify-out/

# Local screenshot scratch - browser profiles and captured PNGs
.shots/

# Local scratch bin - files moved out of the way, never committed
.trash/

# What `node tools/server.js` writes - one player's scenarios and recordings
saves/

# Design notes and handovers - written for people, not shipped with the game
docs/

# Visual scratch - HTML sketches, generated PNGs and the scripts that make them
mockups/

# Sandbox traces - generated, regenerated by `node tools/sandbox/sandbox.js --trace`
tools/sandbox/out/

# Panel read dump - generated scratch
docs-panel-reads.json
